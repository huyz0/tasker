---
name: standards-manage
description: Discovers tribal knowledge from the codebase, writes it as a token-efficient standard in .specs/standards/, and keeps index.yml consistent. Use when codifying a convention, adding a rule, or when the standards index is out of sync.
---

<!-- generated by skill-forge sync — edit .agents/, not this file -->

The full playbook lives at `.agents/skills/standards-manage/SKILL.md` — the portable,
agent-agnostic source of truth for this repository.

Read that file and execute it exactly. Default to interactive mode unless the
caller explicitly asked for autonomous execution.
