---
name: capture-belief
description: Record durable facts, conventions, and gotchas discovered while working a Tasker task into shared memory, so the next agent or person doesn't have to rediscover them. Use whenever you learn something about a project that would still be true and useful after this task closes.
---

<!-- generated by skill-forge sync — edit .agents/, not this file -->

The full playbook lives at `.agents/skills/capture-belief/SKILL.md` — the portable,
agent-agnostic source of truth for this repository.

Read that file and execute it exactly. Default to interactive mode unless the
caller explicitly asked for autonomous execution.
