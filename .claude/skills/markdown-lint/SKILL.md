---
name: markdown-lint
description: Lints Markdown and validates embedded Mermaid blocks deterministically via a bundled script. Use after generating or editing any Markdown artifact, when a machine-verifiable pass/fail is needed rather than a reading.
---

<!-- generated by skill-forge sync — edit .agents/, not this file -->

The full playbook lives at `.agents/skills/markdown-lint/SKILL.md` — the portable,
agent-agnostic source of truth for this repository.

Read that file and execute it exactly. Default to interactive mode unless the
caller explicitly asked for autonomous execution.
