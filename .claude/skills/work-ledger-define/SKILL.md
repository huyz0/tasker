---
name: work-ledger-define
description: Interactively defines the work-ledger.yml to specify if artifacts are tracked in git or externally. Use when interactively setting up tracking ledgers for work items.
---

<!-- generated by skill-forge sync — edit .agents/, not this file -->

The full playbook lives at `.agents/skills/work-ledger-define/SKILL.md` — the portable,
agent-agnostic source of truth for this repository.

Read that file and execute it exactly. Default to interactive mode unless the
caller explicitly asked for autonomous execution.
