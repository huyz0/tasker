---
description: Add, split, re-scope or re-sequence delivery milestones
argument-hint: "[what changed, e.g. 'add SSO milestone after M04']"
---

Read `.agents/skills/milestone-plan/SKILL.md` and execute its instructions
exactly in **INTERACTIVE** mode. Confirm the goal statement and exit criteria
with the user before writing anything. Commit the planning change on its own,
touching no source files.

Requested change: $ARGUMENTS
