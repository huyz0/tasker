---
description: Standards Manage
---

# Standards Manage

**Delegate**: `.agents/skills/standards-manage/SKILL.md`
**Action**: Read and execute standard instructions exactly in INTERACTIVE mode. Use AskUserQuestion.
**Argument**: Optional mode (`discover`, `create`, `index`) and topic. Defaults to `discover`.
