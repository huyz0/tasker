---
description: Milestone Deliver
---

# Milestone Deliver
**Delegate**: `.agents/skills/milestone-deliver/SKILL.md`
**Action**: Read and execute standard instructions exactly in INTERACTIVE mode. Use AskUserQuestion before each task and before each commit.
**Argument**: Optional milestone id (e.g. `M03`). Defaults to `active_milestone` in `.milestones/STATE.md`.
