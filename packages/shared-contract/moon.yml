$schema: https://moonrepo.dev/schemas/project.json
language: system

tasks:
  format:
    command: 'npx tsp format --check "**/*.tsp"'
    inputs:
      - '**/*.tsp'

  compile:
    script: 'npx tsp compile main.tsp && npx buf generate'
    inputs:
      - '**/*.tsp'
      - 'package.json'
    outputs:
      - 'tsp-output'
      - 'gen'
