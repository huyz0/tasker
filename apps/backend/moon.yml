tasks:
  build:
    command: 'bun build ./index.ts --target bun --outdir dist'
  build-standalone:
    command: 'bun run build:standalone'
    inputs:
      - 'index.ts'
      - 'db.ts'
      - 'schema*.ts'
    outputs:
      - 'tasker-standalone'
